import 'package:flutter/material.dart';

class Simulacao extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Simulação'),
      ),
      body: Text('Seu saldo é XXX'),
    );
  }
}
